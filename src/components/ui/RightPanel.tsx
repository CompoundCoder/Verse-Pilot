import React from 'react';

const RightPanel: React.FC = () => {
  return (
    <div className="h-full bg-bg-secondary rounded-lg flex items-center justify-center">
      <span className="text-text-secondary">Right Panel</span>
    </div>
  );
};

export default RightPanel; 