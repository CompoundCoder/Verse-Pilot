import React from 'react';

const Viewer: React.FC = () => {
  return (
    <div className="h-full w-full flex items-center justify-center">
      <span className="text-text-secondary">Viewer</span>
    </div>
  );
};

export default Viewer; 